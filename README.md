# JDA Clone (Next.js 14, App Router) — FIX ESLint 8

Este paquete ya viene con **ESLint 8.57.0** para evitar el conflicto con `eslint-config-next@14.2.12`.

## Pasos
```bash
npm i
cp .env.example .env.local
# Editá SHEET_CSV_URL y BUSINESS_WAPP
npm run dev
```
